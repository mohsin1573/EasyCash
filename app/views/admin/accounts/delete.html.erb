
<h1>Delete Account - <%= @user.email %></h1>

<p>Are you sure you want to delete this user's account?</p>

<%= link_to 'Cancel', admin_account_path(@user.account) %>
<%= link_to 'Delete', admin_account_path(@user.account), method: :delete, data: { confirm: 'Are you sure?' } %>
